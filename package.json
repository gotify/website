{
  "name": "gotify-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "format": "prettier '**/*.md' '.vitepress/**/*.{ts,css}' --write",
    "format:test": "prettier '**/*.md' '.vitepress/**/*.{ts,css}' --list-different"
  },
  "dependencies": {
    "vitepress": "^1.6.0",
    "prettier": "^3.3.3"
  }
}
